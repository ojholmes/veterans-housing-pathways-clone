# Veterans Housing Pathways — Frontend

This is a minimal mobile-first React dashboard scaffold for the Veterans Housing Pathways app.

Quick start (PowerShell):

```powershell
cd c:\Users\majho\OneDrive\Documents\GitHub\veterans-housing-pathways-clone\frontend
npm install
npm run dev
```

The frontend expects the backend API to be available at `http://localhost:4000` by default. Adjust `FRONTEND_API_BASE` in `src/services/api.js` if needed.
